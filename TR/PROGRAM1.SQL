CREATE OR REPLACE TRIGGER T1

	BEFORE DELETE
	ON EMP
	FOR EACH ROW WHEN(OLD.ESAL<15000)

BEGIN

	IF(DELETING) THEN
	RAISE_APPLICATION_ERROR(-20012,'YOU CANNOT DELETE SINCE SALARY IS LESS THAN 5000 RS');
	END IF;

END T1;